#include<Stdio.h>
int main()
{
int a,b;
// here the code will take a lot of time to run so pls wait patiently!!!
printf("enter the number you want even numbers to be printed up to \t");
scanf("%d",&a);
for(b=0;b<=a;b=b+2)
{
printf("%d \n",b);
}
return 0;
}
